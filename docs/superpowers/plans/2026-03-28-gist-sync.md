# GitHub Gist 多端同步功能实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为羽毛球穿线追踪器添加基于 GitHub Gist 的手动云端同步功能，支持多设备间数据同步。

**Architecture:** 在现有数据管理弹窗中添加"云端同步"设置区域。使用 GitHub Gist API 创建/读取/更新私有 Gist 存储数据。同步时比较本地 `lastModified` 和 Gist 的 `updated_at` 时间戳，较新的一方覆盖较旧的。

**Tech Stack:** 纯原生 JavaScript，GitHub REST API (Gist)，localStorage。

**Spec:** `docs/superpowers/specs/2026-03-28-gist-sync-design.md`

---

## 文件变更映射

| 文件 | 操作 | 职责 |
|------|------|------|
| `app.js` | 修改 | 添加 Gist API 交互逻辑、Toast 提示、清理死代码 |
| `index.html` | 修改 | 添加同步 UI 区域、Toast 容器、CSS 样式 |

---

### Task 1: 清理残留死代码

**Files:**
- Modify: `app.js`

清理所有已失效的同步相关代码，为重新实现做准备。

- [ ] **Step 1: 移除 5 处 `syncData()` 调用**

在 `app.js` 中删除以下位置的代码块：

第 101-104 行 (`addRacket` 函数)：
```javascript
    // 自动同步
    if (settings.githubToken) {
        syncData();
    }
```

第 115-118 行 (`deleteRacket` 函数)：
```javascript
        if (settings.githubToken) {
            syncData();
        }
```

第 135-138 行 (`markAsBroken` 函数)：
```javascript
        if (settings.githubToken) {
            syncData();
        }
```

第 180-183 行 (`addRecord` 函数)：
```javascript
    if (settings.githubToken) {
        syncData();
    }
```

第 192-195 行 (`deleteRecord` 函数)：
```javascript
            if (settings.githubToken) {
                syncData();
            }
```

- [ ] **Step 2: 移除空的 `updateSyncStatus` 函数**

删除 `app.js` 第 391-393 行：
```javascript
function updateSyncStatus() {
    // 本地模式，无需显示同步状态
}
```

并从 `updateUI()` 函数（第 232 行）中删除对它的调用：
```javascript
    updateSyncStatus();
```

- [ ] **Step 3: 更新文件头注释**

将 `app.js` 第 1-4 行：
```javascript
/**
 * 羽线追踪 - 羽毛球球线消耗记录应用
 * 使用 GitHub Gist 实现免费云端同步
 */
```
改为：
```javascript
/**
 * 羽线追踪 - 羽毛球球线消耗记录应用
 */
```

- [ ] **Step 4: 在浏览器中验证应用正常工作**

打开 `index.html`，确认：
- 页面正常加载，无 JS 错误
- 添加球拍、记录使用、标记断线、删除功能正常
- 数据管理弹窗正常打开关闭
- 导入导出功能正常

- [ ] **Step 5: 提交**

```bash
git add app.js
git commit -m "chore: clean up dead sync code remnants"
```

---

### Task 2: 添加 `lastModified` 时间戳追踪

**Files:**
- Modify: `app.js`

为数据添加最后修改时间戳，作为同步时的冲突判断依据。

- [ ] **Step 1: 在 `appData` 初始化中添加 `lastModified` 字段**

将 `app.js` 中 `appData` 的初始值（第 11-14 行）：
```javascript
let appData = {
    rackets: [],
    lastSync: null
};
```
改为：
```javascript
let appData = {
    rackets: [],
    lastModified: null
};
```

- [ ] **Step 2: 修改 `saveData()` 函数，每次保存时更新 `lastModified`**

将 `app.js` 中的 `saveData` 函数（第 59-62 行）：
```javascript
function saveData() {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(appData));
    updateUI();
}
```
改为：
```javascript
function saveData() {
    appData.lastModified = new Date().toISOString();
    localStorage.setItem(STORAGE_KEY, JSON.stringify(appData));
    updateUI();
}
```

- [ ] **Step 3: 更新 `clearAllData()` 中的初始数据结构**

将 `app.js` 中 `clearAllData` 函数里的 `appData` 重置（第 527-530 行）：
```javascript
            appData = {
                rackets: [],
                lastSync: null
            };
```
改为：
```javascript
            appData = {
                rackets: [],
                lastModified: null
            };
```

- [ ] **Step 4: 在浏览器中验证**

打开页面，添加一个球拍，然后在浏览器控制台运行 `JSON.parse(localStorage.getItem('badmintonStringTracker_data')).lastModified` 确认返回一个 ISO 时间字符串。

- [ ] **Step 5: 提交**

```bash
git add app.js
git commit -m "feat: add lastModified timestamp tracking for sync"
```

---

### Task 3: 添加 Toast 提示组件

**Files:**
- Modify: `index.html` — 添加 Toast HTML 容器和 CSS 样式
- Modify: `app.js` — 添加 `showToast()` 函数

统一的 Toast 提示系统，用于显示同步状态和其他操作反馈。

- [ ] **Step 1: 在 `index.html` 的 `</body>` 前添加 Toast 容器**

在 `index.html` 的 `<script src="app.js"></script>` 行之前添加：
```html
    <!-- Toast 提示 -->
    <div id="toastContainer" style="position: fixed; top: 1.5rem; right: 1.5rem; z-index: 2000; display: flex; flex-direction: column; gap: 0.5rem;"></div>
```

- [ ] **Step 2: 在 `index.html` 的 `<style>` 标签内（`</style>` 之前）添加 Toast CSS**

在 `index.html` 中 `</style>` 标签之前添加：
```css
        /* Toast 提示 */
        .toast {
            padding: 0.875rem 1.25rem;
            border-radius: 10px;
            font-size: 0.9rem;
            font-weight: 500;
            backdrop-filter: blur(10px);
            animation: toastIn 0.3s ease forwards;
            max-width: 320px;
            word-break: break-word;
        }

        .toast.success {
            background: rgba(57, 255, 20, 0.15);
            border: 1px solid rgba(57, 255, 20, 0.4);
            color: var(--neon-green);
        }

        .toast.error {
            background: rgba(255, 68, 68, 0.15);
            border: 1px solid rgba(255, 68, 68, 0.4);
            color: #ff4444;
        }

        .toast.info {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: rgba(255, 255, 255, 0.7);
        }

        .toast.hide {
            animation: toastOut 0.3s ease forwards;
        }

        @keyframes toastIn {
            from { opacity: 0; transform: translateX(40px); }
            to { opacity: 1; transform: translateX(0); }
        }

        @keyframes toastOut {
            from { opacity: 1; transform: translateX(0); }
            to { opacity: 0; transform: translateX(40px); }
        }
```

- [ ] **Step 3: 在 `app.js` 的工具函数区域添加 `showToast()` 函数**

在 `app.js` 末尾（`formatDate` 函数之后）添加：
```javascript

// ==================== Toast 提示 ====================

function showToast(message, type = 'info') {
    const container = document.getElementById('toastContainer');
    const toast = document.createElement('div');
    toast.className = `toast ${type}`;
    toast.textContent = message;
    container.appendChild(toast);

    setTimeout(() => {
        toast.classList.add('hide');
        setTimeout(() => toast.remove(), 300);
    }, 3000);
}
```

- [ ] **Step 4: 在浏览器中验证 Toast**

打开浏览器控制台，运行 `showToast('测试提示', 'success')`，确认右上角出现绿色 Toast 提示并在 3 秒后消失。

- [ ] **Step 5: 提交**

```bash
git add index.html app.js
git commit -m "feat: add toast notification component"
```

---

### Task 4: 添加 Gist API 交互函数

**Files:**
- Modify: `app.js`

实现与 GitHub Gist API 的所有交互逻辑：创建、读取、更新 Gist。

- [ ] **Step 1: 在 `app.js` 中添加 Gist API 函数**

在 `app.js` 的 `saveSettingsLocal()` 函数之后（`// ==================== 球拍管理 ====================` 注释之前）添加：
```javascript

// ==================== Gist 同步 ====================

const GIST_API = 'https://api.github.com/gists';
const GIST_FILENAME = 'badminton-string-tracker.json';
const GIST_DESCRIPTION = 'Badminton String Tracker Data';

async function gistApi(method, path, body = null) {
    const options = {
        method,
        headers: {
            'Authorization': `Bearer ${settings.githubToken}`,
            'Content-Type': 'application/json'
        }
    };
    if (body) {
        options.body = JSON.stringify(body);
    }
    const response = await fetch(`${GIST_API}${path}`, options);
    return response;
}

async function createGist() {
    const response = await gistApi('POST', '', {
        description: GIST_DESCRIPTION,
        public: false,
        files: {
            [GIST_FILENAME]: {
                content: JSON.stringify(appData, null, 2)
            }
        }
    });

    if (!response.ok) {
        throw handleGistError(response.status);
    }

    const gist = await response.json();
    settings.gistId = gist.id;
    saveSettingsLocal();
    return gist;
}

async function readGist() {
    const response = await gistApi('GET', `/${settings.gistId}`);

    if (!response.ok) {
        throw handleGistError(response.status);
    }

    const gist = await response.json();
    const file = gist.files[GIST_FILENAME];
    if (!file) {
        throw new Error('Gist 中未找到数据文件');
    }

    return {
        data: JSON.parse(file.content),
        updatedAt: gist.updated_at
    };
}

async function updateGist() {
    const response = await gistApi('PATCH', `/${settings.gistId}`, {
        description: GIST_DESCRIPTION,
        files: {
            [GIST_FILENAME]: {
                content: JSON.stringify(appData, null, 2)
            }
        }
    });

    if (!response.ok) {
        throw handleGistError(response.status);
    }
}

function handleGistError(status) {
    switch (status) {
        case 401:
            return new Error('Token 无效或已过期，请重新配置');
        case 403:
            return new Error('请求过于频繁，请稍后再试');
        case 404:
            settings.gistId = '';
            saveSettingsLocal();
            return new Error('远程 Gist 不存在，请重新连接');
        default:
            return new Error(`同步失败 (HTTP ${status})`);
    }
}

// 连接 Gist
async function connectGist() {
    const tokenInput = document.getElementById('gistTokenInput');
    const token = tokenInput.value.trim();
    if (!token) {
        showToast('请输入 GitHub Token', 'error');
        return;
    }

    try {
        settings.githubToken = token;
        await createGist();
        updateSyncUI();
        showToast('连接成功！Gist 已创建并上传数据', 'success');
    } catch (error) {
        settings.githubToken = '';
        settings.gistId = '';
        saveSettingsLocal();
        showToast(error.message, 'error');
    }
}

// 断开连接
function disconnectGist() {
    settings.githubToken = '';
    settings.gistId = '';
    saveSettingsLocal();
    updateSyncUI();
    showToast('已断开云端连接', 'info');
}

// 手动同步
async function syncWithGist() {
    if (!settings.githubToken || !settings.gistId) {
        showToast('请先连接 GitHub Gist', 'error');
        return;
    }

    const syncBtn = document.getElementById('syncBtn');
    const originalText = syncBtn.textContent;
    syncBtn.disabled = true;
    syncBtn.textContent = '同步中...';

    try {
        const remote = await readGist();
        const localTime = appData.lastModified ? new Date(appData.lastModified).getTime() : 0;
        const remoteTime = new Date(remote.updatedAt).getTime();
        const diff = Math.abs(localTime - remoteTime);

        if (diff < 2000) {
            // 时间差 < 2秒，视为相同
            showToast('数据已是最新', 'info');
        } else if (localTime > remoteTime) {
            // 本地较新，上传
            await updateGist();
            showToast('数据已上传到云端', 'success');
        } else {
            // 远程较新，下载
            appData = remote.data;
            saveData();
            showToast('已从云端同步最新数据', 'success');
        }
    } catch (error) {
        if (error instanceof TypeError && error.message.includes('fetch')) {
            showToast('网络连接失败，请检查网络', 'error');
        } else {
            showToast(error.message, 'error');
        }
    } finally {
        syncBtn.disabled = false;
        syncBtn.textContent = originalText;
    }
}
```

- [ ] **Step 2: 在浏览器中验证语法无误**

打开浏览器控制台，确认没有 JS 语法错误（页面正常加载），运行 `typeof createGist` 应返回 `'function'`。

- [ ] **Step 3: 提交**

```bash
git add app.js
git commit -m "feat: add GitHub Gist API interaction functions"
```

---

### Task 5: 添加云端同步 UI 和 CSS

**Files:**
- Modify: `index.html` — 添加同步设置 HTML 和 CSS
- Modify: `app.js` — 添加 `updateSyncUI()` 和打开设置时更新 UI 的逻辑

在数据管理弹窗中添加云端同步设置区域，并根据连接状态显示不同内容。

- [ ] **Step 1: 在 `index.html` 的 CSS 中添加同步 UI 相关样式**

在 `index.html` 的 `</style>` 标签前添加：
```css
        /* 同步设置 */
        .sync-connected-info {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-bottom: 0.75rem;
            font-size: 0.85rem;
            color: rgba(255,255,255,0.6);
        }

        .sync-connected-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: var(--neon-green);
            box-shadow: 0 0 8px var(--neon-green);
            flex-shrink: 0;
        }

        .token-toggle {
            position: absolute;
            right: 0.75rem;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            color: rgba(255,255,255,0.4);
            cursor: pointer;
            font-size: 0.85rem;
            padding: 0.25rem;
        }

        .token-toggle:hover {
            color: rgba(255,255,255,0.7);
        }

        .token-input-wrapper {
            position: relative;
        }

        .token-input-wrapper .form-input {
            padding-right: 2.5rem;
        }

        .btn-sync {
            background: linear-gradient(135deg, #6366f1, #8b5cf6);
            color: #fff;
        }

        .btn-sync:hover {
            box-shadow: 0 5px 20px rgba(99, 102, 241, 0.4);
            transform: scale(1.02);
        }

        .btn-sync:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
            box-shadow: none;
        }
```

- [ ] **Step 2: 修改数据管理模态框，添加云端同步区域**

在 `index.html` 的数据管理模态框 (`id="settingsModal"`) 中，将整个 `<div class="modal-body">` 内容替换为：

```html
            <div class="modal-body">
                <div class="settings-section">
                    <div class="settings-title">☁️ 云端同步</div>
                    <div id="syncDisconnected">
                        <p style="font-size: 0.9rem; color: rgba(255,255,255,0.7); line-height: 1.6; margin-bottom: 1rem;">
                            使用 GitHub Gist 免费同步数据到云端，支持多设备间同步。
                        </p>
                        <div class="form-group">
                            <label class="form-label">GitHub Personal Access Token</label>
                            <div class="token-input-wrapper">
                                <input type="password" class="form-input token-input" id="gistTokenInput"
                                    placeholder="ghp_xxxxxxxxxxxx">
                                <button type="button" class="token-toggle" onclick="toggleTokenVisibility()">显示</button>
                            </div>
                            <div class="form-hint">只需 gist 权限。<a href="https://github.com/settings/tokens/new?scopes=gist" target="_blank" style="color: var(--electric-blue);">点击创建 Token</a></div>
                        </div>
                        <div class="form-actions">
                            <button type="button" class="btn btn-sync btn-full" onclick="connectGist()">连接</button>
                        </div>
                    </div>
                    <div id="syncConnected" style="display: none;">
                        <div class="sync-connected-info">
                            <span class="sync-connected-dot"></span>
                            <span>已连接 · Gist ID: <code id="syncGistId" style="font-size: 0.8rem; color: var(--electric-blue);"></code></span>
                        </div>
                        <div class="form-actions">
                            <button type="button" class="btn btn-sync btn-full" id="syncBtn" onclick="syncWithGist()">🔄 同步数据</button>
                        </div>
                        <div style="margin-top: 0.75rem;">
                            <button type="button" class="btn btn-danger btn-full" onclick="disconnectGist()" style="font-size: 0.8rem; padding: 0.5rem;">断开连接</button>
                        </div>
                    </div>
                </div>

                <div class="settings-section" style="margin-top: 1.5rem; padding-top: 1.5rem; border-top: 1px solid var(--glass-border);">
                    <div class="settings-title">导出数据</div>
                    <p style="font-size: 0.9rem; color: rgba(255,255,255,0.7); line-height: 1.6; margin-bottom: 1rem;">
                        将数据导出为 JSON 格式，可以复制文本或下载文件。
                    </p>
                    <div class="form-actions">
                        <button type="button" class="btn btn-primary btn-full" onclick="exportDataToClipboard()">📋 复制 JSON</button>
                        <button type="button" class="btn btn-secondary btn-full" onclick="exportDataToFile()">💾 下载文件</button>
                    </div>
                </div>

                <div class="settings-section" style="margin-top: 1.5rem; padding-top: 1.5rem; border-top: 1px solid var(--glass-border);">
                    <div class="settings-title">导入数据</div>
                    <p style="font-size: 0.9rem; color: rgba(255,255,255,0.7); line-height: 1.6; margin-bottom: 1rem;">
                        粘贴 JSON 数据文本，或上传备份文件恢复数据。
                    </p>
                    <textarea class="form-input" id="importDataText" rows="6" placeholder='粘贴 JSON 数据，例如：
{
  "rackets": [...],
  "lastModified": "..."
}' style="font-family: monospace; font-size: 0.8rem; margin-bottom: 0.75rem;"></textarea>
                    <div class="form-actions">
                        <button type="button" class="btn btn-primary btn-full" onclick="importDataFromText()">📋 粘贴导入</button>
                        <button type="button" class="btn btn-secondary btn-full" onclick="document.getElementById('importFileInput').click()">📁 上传文件</button>
                    </div>
                    <input type="file" id="importFileInput" accept=".json" style="display: none;" onchange="importDataFromFile(this)">
                </div>

                <div class="settings-section" style="margin-top: 1.5rem; padding-top: 1.5rem; border-top: 1px solid var(--glass-border);">
                    <div class="settings-title">危险区域</div>
                    <button type="button" class="btn btn-danger btn-full" onclick="clearAllData()">🗑️ 清空所有数据</button>
                </div>
            </div>
```

- [ ] **Step 3: 在 `app.js` 中添加 `updateSyncUI()` 和 `toggleTokenVisibility()` 函数**

在 `app.js` 的 Gist 同步区域（`syncWithGist` 函数之后）添加：
```javascript

function updateSyncUI() {
    const disconnected = document.getElementById('syncDisconnected');
    const connected = document.getElementById('syncConnected');
    const gistIdEl = document.getElementById('syncGistId');

    if (settings.githubToken && settings.gistId) {
        disconnected.style.display = 'none';
        connected.style.display = 'block';
        gistIdEl.textContent = settings.gistId.substring(0, 8) + '...';
    } else {
        disconnected.style.display = 'block';
        connected.style.display = 'none';
    }
}

function toggleTokenVisibility() {
    const input = document.getElementById('gistTokenInput');
    const btn = input.parentElement.querySelector('.token-toggle');
    if (input.type === 'password') {
        input.type = 'text';
        btn.textContent = '隐藏';
    } else {
        input.type = 'password';
        btn.textContent = '显示';
    }
}
```

- [ ] **Step 4: 修改 `openSettings()` 函数，打开时更新同步 UI**

将 `app.js` 中的 `openSettings` 函数：
```javascript
function openSettings() {
    document.getElementById('settingsModal').classList.add('active');
}
```
改为：
```javascript
function openSettings() {
    document.getElementById('settingsModal').classList.add('active');
    updateSyncUI();
}
```

- [ ] **Step 5: 在浏览器中验证 UI**

打开页面，点击"数据管理"按钮，确认：
- 弹窗顶部显示"☁️ 云端同步"区域，包含 Token 输入框和"连接"按钮
- 下方仍有导出、导入、危险区域
- 点击 Token 输入框旁的"显示/隐藏"按钮可切换密码可见性
- 页面整体布局无变形

- [ ] **Step 6: 提交**

```bash
git add index.html app.js
git commit -m "feat: add cloud sync UI in data management modal"
```

---

### Task 6: 端到端集成测试

**Files:**
- 无文件变更

使用真实 GitHub Token 进行完整流程验证。

- [ ] **Step 1: 测试首次连接流程**

1. 打开数据管理弹窗
2. 输入一个有 `gist` 权限的 GitHub Token
3. 点击"连接"
4. 预期：显示"连接成功！Gist 已创建并上传数据"的绿色 Toast
5. 确认 UI 切换为已连接状态，显示 Gist ID
6. 到 GitHub Gist 页面 (`https://gist.github.com/`) 确认私有 Gist 已创建

- [ ] **Step 2: 测试同步 — 上传场景**

1. 在已连接状态下，添加一个新球拍
2. 打开数据管理弹窗，点击"同步数据"
3. 预期：显示"数据已上传到云端"的绿色 Toast
4. 到 GitHub 查看 Gist 内容，确认数据已更新

- [ ] **Step 3: 测试同步 — 下载场景**

1. 在 GitHub Gist 页面直接编辑 JSON，修改某个值（如改球拍名称）
2. 记下 Gist 的 `updated_at` 应该变新了
3. 回到应用，点击"同步数据"
4. 预期：显示"已从云端同步最新数据"，本地数据被更新

- [ ] **Step 4: 测试同步 — 无变更场景**

1. 不做任何修改，再次点击"同步数据"
2. 预期：显示"数据已是最新"的灰色 Toast

- [ ] **Step 5: 测试错误场景**

1. 断开连接，然后手动在 localStorage 中设置一个无效的 gistId
2. 重新设置正确的 Token，尝试同步
3. 预期：显示"Gist 不存在"的错误提示

- [ ] **Step 6: 测试断开连接**

1. 点击"断开连接"
2. 预期：显示"已断开云端连接"的 Toast
3. UI 切回未连接状态，显示 Token 输入框
4. 确认 localStorage 中 `githubToken` 和 `gistId` 已清空

- [ ] **Step 7: 测试现有功能未受影响**

1. 添加球拍 → 正常
2. 记录使用 → 正常
3. 标记断线 → 正常
4. 删除球拍 → 正常
5. 复制 JSON → 正常
6. 下载文件 → 正常
7. 导入数据 → 正常
8. 清空数据 → 正常

- [ ] **Step 8: 提交最终状态**

如果有任何修复，提交修复。如果没有，跳过此步。

```bash
git add -A
git commit -m "fix: adjustments from end-to-end testing"
```
